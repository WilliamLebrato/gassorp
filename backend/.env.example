# Database Configuration
DATABASE_URL=sqlite:///./gsp.db

# Node Agent Configuration (for Docker orchestration)
NODE_URL=http://node-agent:8001
NODE_SECRET=dev-secret-change-in-production

# JWT Secret (IMPORTANT: Change this in production!)
SECRET_KEY=your-secret-key-change-this-in-production-use-openssl-rand-hex-32

# Webhook Secret
WEBHOOK_SECRET=shared-webhook-secret

# Google Cloud Storage (for backups)
GOOGLE_CLOUD_BUCKET=gsp-backups

# ============================================
# OAUTH CONFIGURATION - REQUIRED FOR LOGIN
# ============================================

# Google OAuth2 Configuration
# Get credentials from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
# IMPORTANT: This redirect URI must be added to Google OAuth consent screen
GOOGLE_REDIRECT_URI=http://localhost:8000/auth/callback/google

# Microsoft OAuth2 Configuration
# Get credentials from: https://portal.azure.com/#view/Microsoft_AAD_RegisteredApps/ApplicationsListBlade
MICROSOFT_CLIENT_ID=your-microsoft-client-id
MICROSOFT_CLIENT_SECRET=your-microsoft-client-secret
# IMPORTANT: This redirect URI must be added to Microsoft app registration
MICROSOFT_REDIRECT_URI=http://localhost:8000/auth/callback/microsoft

# Development Settings
# DEV_MODE=true enables the DEV LOGIN button on frontend (bypasses OAuth)
DEV_MODE=true
